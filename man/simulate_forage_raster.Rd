% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/vital_rates.R
\name{simulate_forage_raster}
\alias{simulate_forage_raster}
\title{Simulate a spatiotemporally autocorrelated forage raster stack}
\usage{
simulate_forage_raster(
  nrow = 100L,
  ncol = 100L,
  res = 30,
  xmin = 5e+05,
  ymin = 4800000,
  smooth_sigma = 5,
  peak_mean = 180,
  end_mean = 180,
  max_val = 500,
  seed = NULL
)
}
\arguments{
\item{nrow}{Integer. Number of raster rows. Default 100.}

\item{ncol}{Integer. Number of raster columns. Default 100.}

\item{res}{Numeric. Cell resolution in metres. Default 30.}

\item{xmin}{Numeric. Left edge of raster extent in UTM Zone 12N metres.
Default 500000.}

\item{ymin}{Numeric. Bottom edge of raster extent in UTM Zone 12N metres.
Default 4800000.}

\item{smooth_sigma}{Numeric. Standard deviation of the Gaussian smoothing
kernel in cells, controlling spatial autocorrelation. Default 5.}

\item{peak_mean}{Numeric. Target mean forage at peak on day 1 (kg/ha).
Default 700.}

\item{end_mean}{Numeric. Target mean forage on the final simulation day
(kg/ha). Default 200.}

\item{max_val}{Numeric. Hard upper ceiling on forage values (kg/ha).
Default 1200.}

\item{seed}{Integer or NULL. Random seed for reproducibility. Default NULL.}
}
\value{
A \code{terra::SpatRaster} with one layer per simulation day, named
by date (e.g. \code{"2025-07-01"}). Values are in kg/ha. CRS is set to
the value of \code{get_param("epsg")}.
}
\description{
Generates a daily \code{SpatRaster} of forage biomass (kg/ha)
over the simulation window. Spatial autocorrelation is imposed by Gaussian
smoothing of random noise. Temporal decline follows a logistic decay curve
with a mean of \code{peak_mean} on the first day of the simulation (July 1)
and a mean of \code{end_mean} by the end of the season (Oct 31).
Values are clipped to [0, max_val] kg/ha.
}
